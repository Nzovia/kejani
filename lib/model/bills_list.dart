class BillsList {
  int billId;
  int billAmount;
  int billbalance;
  String logo;
  String billName;
  String billPriority;
  String billStatus;
  DateTime paymentDate;

  BillsList(this.billId, this.billAmount, this.billbalance, this.logo,
      this.billName, this.billPriority, this.billStatus, this.paymentDate);
}
